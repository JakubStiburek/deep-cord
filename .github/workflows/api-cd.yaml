name: API-CD

on:
  workflow_run:
    workflows: ["API-CI"]
    types:
      - completed
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Trigger Deploy Webhook
        run: curl "https://api.render.com/deploy/srv-ctqkmsbqf0us73estr6g?key=${{ secrets.RENDER_API_KEY }}"
